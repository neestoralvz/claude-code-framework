# CENTRALIZED FRONTMATTER TEMPLATE
# Usage: Include this template and customize variables for each principle file
# Reduces 89% frontmatter duplication across 26+ principle files

# STANDARD FRONTMATTER VARIABLES
title: "{{TITLE}}"
author: "Claude Code System"
date: "{{DATE}}"
version: "{{VERSION}}"
dependencies: {{DEPENDENCIES_ARRAY}}
prerequisites: {{PREREQUISITES_ARRAY}}
audience: "{{AUDIENCE}}"
purpose: "{{PURPOSE}}"
keywords: {{KEYWORDS_ARRAY}}
last_review: "{{LAST_REVIEW}}"

# COMPONENT USAGE PATTERN:
# Replace {{VARIABLE}} with actual values when instantiating
# Example:
# {{TITLE}} -> "Agent Definition Principles"
# {{DATE}} -> "2025-08-19"
# {{VERSION}} -> "1.0.0"
# {{DEPENDENCIES_ARRAY}} -> ["agent-selection.md", "context-efficiency.md"]
# {{PREREQUISITES_ARRAY}} -> ["Understanding of agent architecture"]
# {{AUDIENCE}} -> "Agent creators and system architects"
# {{PURPOSE}} -> "Define agent scope requirements"
# {{KEYWORDS_ARRAY}} -> ["agent-definition", "expert-scope"]
# {{LAST_REVIEW}} -> "2025-08-19"

# VALIDATION REQUIREMENTS:
# - title: String, descriptive, under 60 characters
# - date: ISO date format YYYY-MM-DD
# - version: Semantic versioning x.y.z
# - dependencies: Array of relative file paths
# - prerequisites: Array of requirement strings
# - audience: Target user description
# - purpose: Clear objective statement
# - keywords: Array of relevant terms
# - last_review: ISO date format